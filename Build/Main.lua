local script = Spawns
local Tracers = Tracers Api.lua;
local ESP = ESP Api.lua;
local Service = {
  local Once = ESP or Tracers Once = false;
  local Modules = {
    local ToggleRuns = ESP and Tracers;
  }
} wait(1) while game:GetService("RunService").RenderStepped:wait()
return tonumber;
end
local function ModuleFunc(...)
		return {...} or return (...);
end
if ModuleFunc(...) then
  local Run =  game:GetService("RunService")
Run:Destroy()
Spawns.Parent
end
